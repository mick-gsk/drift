# DEPRECATED — release-drafter is no longer used.
# GitHub Releases are now created directly from CHANGELOG.md content
# in .github/workflows/release.yml (step "Create GitHub Release from CHANGELOG").
# This file is kept to prevent release-drafter from auto-activating if
# the GitHub App is still installed on this repository.
