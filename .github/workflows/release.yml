name: Release (python-semantic-release)

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  release:
    name: Semantic Release
    # NOTE: Uses self-hosted Windows runner (GitHub-hosted runners temporarily unavailable).
    # Migration to ubuntu-latest tracked in: https://github.com/sauremilk/drift/issues/90
    runs-on: self-hosted
    # Skip release-commit pushes (safety net; GITHUB_TOKEN pushes
    # already don't trigger workflows, but guard explicitly).
    if: "!startsWith(github.event.head_commit.message, 'chore: Release ')"

    outputs:
      released: ${{ steps.release.outputs.released }}
      version: ${{ steps.release.outputs.version }}
      tag: ${{ steps.release.outputs.tag }}

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install python-semantic-release
        shell: powershell
        run: pip install python-semantic-release

      - name: Python Semantic Release – Version
        id: release
        shell: powershell
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PYTHONUTF8: "1"
        run: |
          # Run version detection and local commit+tag (no push yet)
          semantic-release -v version --no-push --no-vcs-release --skip-build

          # Check if a new tag was created on HEAD.
          # Use cmd to avoid PowerShell NativeCommandError when no exact tag exists.
          $tag = cmd /c "git describe --tags --exact-match HEAD 2>nul"
          if ($LASTEXITCODE -ne 0 -or [string]::IsNullOrWhiteSpace($tag)) {
            Write-Output "No new version - nothing to release."
            "released=false" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
            exit 0
          }

          $version = $tag -replace '^v', ''
          Write-Output "Detected release: tag=$tag version=$version"
          "released=true" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
          "version=$version" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
          "tag=$tag" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append

      - name: Push release commit and tag
        if: steps.release.outputs.released == 'true'
        shell: powershell
        run: git push origin main --tags

      - name: Create GitHub Release
        if: steps.release.outputs.released == 'true'
        shell: powershell
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          $tag = "${{ steps.release.outputs.tag }}"
          gh release create $tag --title $tag --generate-notes --verify-tag

  publish:
    name: Build and publish to PyPI
    needs: release
    if: needs.release.outputs.released == 'true'
    # NOTE: Uses self-hosted Windows runner (GitHub-hosted runners temporarily unavailable).
    # Migration to ubuntu-latest tracked in: https://github.com/sauremilk/drift/issues/90
    runs-on: self-hosted
    environment: pypi

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.release.outputs.tag }}

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build wheel and sdist
        shell: powershell
        run: |
          pip install --upgrade build twine
          python -m build

      - name: Publish to PyPI
        shell: powershell
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: ${{ secrets.PYPI_RELEASE }}
        run: python -m twine upload --non-interactive dist/*

  update-major-tag:
    name: Update major-version tag (e.g. v1 → latest patch)
    needs: [release, publish]
    if: needs.release.outputs.released == 'true'
    # NOTE: Uses self-hosted Windows runner (GitHub-hosted runners temporarily unavailable).
    # Migration to ubuntu-latest tracked in: https://github.com/sauremilk/drift/issues/90
    runs-on: self-hosted
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Move major-version tag
        shell: powershell
        run: |
          $tag = "${{ needs.release.outputs.tag }}"
          if ($tag -notmatch '^v\d+\.\d+\.\d+$') {
            Write-Output "Tag $tag does not match vMAJOR.MINOR.PATCH - skipping major-tag update"
            exit 0
          }
          $major = [regex]::Match($tag, '^v\d+').Value
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag -f $major
          git push -f origin $major
          Write-Output "Moved $major -> $tag"
